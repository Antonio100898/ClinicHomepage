import { HomePageType } from '@/lib/interface'
import React from 'react'
import { WrapperLarge } from '../wrapper'

export default function ContactUs({ data }: HomePageType) {
  return <WrapperLarge>Contact Us</WrapperLarge>
}
